    <div class="container">
      <div class="calendar">
        <div class="month">
          <span class="prev">&#10092;&#10092;&#10092;</span>
          <div class="loginMessage">
            {{#if logged_in}}
            <a href="/signout" id="signOut" class="loginMessageLink">SIGN OUT</a>
            {{/if}}
          </div>
          <div class="date">
            <h1></h1>
            <p id="currentDate"></p>
            <select id="monthList" onchange="getSelectedMonth()"></select>
            <select id="yearList" onchange="getSelectedYear()"></select>
          </div>
          <div class="loginMessage">
            {{#if logged_in}}
            <!-- Pending -->
            <a href="#" class="loginMessageLink">SETTINGS</a>
            {{/if}}
          </div>
          <span class="next">&#10093;&#10093;&#10093;</span>
        </div>
        <div class="weekdays">
          <div>Sunday</div>
          <div>Monday</div>
          <div>Tuesday</div>
          <div>Wednesday</div>
          <div>Thursday</div>
          <div>Friday</div>
          <div>Saturday</div>
        </div>
        <div class="days"></div>
      </div>
    </div>

    <script src="./js/script.js"></script>
